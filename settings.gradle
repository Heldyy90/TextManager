pluginManagement {
	repositories {
		maven { url 'https://maven.fabricmc.net/' }
		mavenCentral()
		gradlePluginPortal()
	}
}

rootProject.name = 'textmanager'

// If you prefer Kotlin DSL, rename to settings.gradle.kts and adjust accordingly.
